---
import Link from "@components/Link.svelte";

interface Props {
  href: string;
  className?: string;
}

const { href, className = `btn btn-ghost text-xl font-bold` } = Astro.props;
---

<Link className={className} href={href} client:load>
  <slot />
</Link>
